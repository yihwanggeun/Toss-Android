<?xml version="1.0" encoding="utf-8"?>
<androidx.coordinatorlayout.widget.CoordinatorLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/container"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingHorizontal="16dp">

    <FrameLayout
        android:id="@+id/main_container"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_weight="1" />

    <com.google.android.material.bottomnavigation.BottomNavigationView
        android:id="@+id/bottom_navigation"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom"
        app:menu= "@menu/bottom_navigation_menu"
        app:itemIconSize="24dp"
        app:itemPaddingBottom="8dp"
        app:itemPaddingTop="8dp"
        app:itemRippleColor="@color/g_07"
        app:labelVisibilityMode="labeled"
        android:background="@drawable/bottom_nav_background"
        app:itemTextAppearanceActive="@style/TextAppearance.Toss.Body.Reg.12"
        app:itemTextAppearanceInactive="@style/TextAppearance.Toss.Body.Reg.12"
        app:itemTextColor="@color/bottom_nav_text_selector"
        app:itemIconTint="@color/bottom_nav_icon_selector"
        />

</androidx.coordinatorlayout.widget.CoordinatorLayout>